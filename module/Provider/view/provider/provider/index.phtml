<h1>Users</h1>

<br />

<p>
    <a href="<?php echo $this->url('Provider/default',array('action'=>'new')); ?>">Novo</a>
</p>

<table class="table">
    <tr>
        <th>ID</th>
        <th>Nome</th>
        <th>Data de Inclusão</th>
        <th>Status</th>
        <th>Ação</th>
    </tr>
    
    <?php foreach($this->data as $entity): ?>
    <tr>
    
        <td><?php echo $entity->getId(); ?></td>
        <td><?php echo $entity->getNome(); ?></td>
        <td><?php echo $entity->getDt()->format('d/m/Y H:i:s'); ?></td>
        <td><?php echo ($entity->getStatus() == 0 ? "Ativo": "Desativado"); ?></td>
        <td>
            <a href="<?php echo $this->url('Provider/default',
                    array('controller'=>'Provider','action'=>'edit','id'=>$entity->getId()));?>">Editar</a>
            
            <a href="<?php echo $this->url('Provider/default',
                    array('controller'=>'Provider','action'=>'delete','id'=>$entity->getId()));?>">Delete</a>
            
        </td>
    </tr>
    <?php endforeach; ?>
</table>
<br />

<?php echo $this->paginationControl($this->data,'Sliding','partials/paginator'); ?>